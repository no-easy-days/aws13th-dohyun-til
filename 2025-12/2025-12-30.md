# Python

## python 기초 문법 공부
- 어제 들었어야 하는 수업을 불가피하게 참여하지 못하는 바람에, 오늘 스스로 공부하는 시간을 많이 가지게 되었다.
- 기본 변수명부터 제어문, 클래스, 모듈등까지 하루에 많은 양을 최대한 흡수하려했다.


### python cli 과제 진행

- model.py
```
class Book:
    def __init__(self, title, author, isbn, is_borrowed = False):
        self.title = title
        self.author = author
        self.isbn = isbn
        self.is_borrowed = is_borrowed
    def __str__(self):
        status = "대출중" if self.is_borrowed else "대출 가능"
        return f'{self.title} {self.author} {self.isbn} {self.is_borrowed}'

class Member:
    def __init__(self, name, phone, borrowed_books):
        self.name = name
        self.phone = phone
        self.borrowed_books = borrowed_books

class Library:
    def __init__(self, books=None, members=None):
        self.books = books if books is not None else []
        self.members = members if members is not None else {}

    def add_book(self, book):
        self.books.append(book)

    def add_member(self, member):
        self.members[member.name] = member

    def borrow_book(self, member_name, isbn):
        if member_name not in self.members:
            print("회원이 존재하지 않습니다.")
        elif self.books[isbn].is_borrowed is False:
            print("책이 대출중입니다.")
        else:
            self.members[member_name].borrowed_books.append(isbn)
            self.books[isbn].is_borrowed = True


    def return_books(self, book):
        self.books.append(book)
        return self
```
----------
- main.py
```
import csv
from enum import member

from models import *

def load_csv(filename, library):
    print("")
    print("[도서 등록 시스템]")
    with open(filename, 'r', encoding='utf-8') as csvfile:
        reader = csv.reader(csvfile)
        header = next(reader)
        for row in reader:
            book = Book(
                title=row[0],
                author=row[1],
                isbn=row[2]
            )
            library.add_book(book)
    print("도서 등록이 완료되었습니다.")

def show_book(library):
    print("")
    print("[도서 목록 시스템]")
    print("등록 완료된 도서 목록 입니다")
    for book in library.books:
        book.__str__()
        print(book)

def register_member(library):
    print("")
    print("[회원 등록 시스템]")
    name = input("회원 이름 : ")
    phone = input("전화번호 : ")

    member = Member(name, phone, [])
    library.add_member(member)
    print(f">> {name}님이 {phone} 번호로 회원 등록을 완료하였습니다.")


def borrow_book(library):
    print("")
    print("[대출 시스템]")
    name = input("회원 이름 : ")
    isbn = input("도서 번호 : ")

    result = Member(name, isbn)
    library.borrow_book(result)
    print(f">> {name}님이 {isbn}번 도서를 대여하였습니다.")

def return_book(library):
    print("")
    print("[반납 시스템]")
    name = input("회원 이름 : ")
    isbn = input("도서 번호 : ")
    result = Member(name, isbn)
    library.return_book(result)
    print(f">> {name}님이 {isbn}번 도서를 반납하였습니다.")

def search_book(library):
    print("")
    print("[검색 시스템]")

def main():

    library = Library()

    print("[System] books.csv 에서 도서 데이터를 불러왔습니다.")
    print("")
    print("=== 도서관 관리 시스템 ===")
    print("1. 도서 등록")
    print("2. 도서 목록")
    print("3. 회원 등록")
    print("4. 대출")
    print("5. 반납")
    print("6. 검색")
    print("7. 종료")

    while True:
        choice = input("메뉴를 선택하세요 : ")
        if choice == "1":
            load_csv('books.csv', library)
            break
        elif choice == "2":
            show_book(library)
            break
        elif choice == "3":
            register_member(library)
            break
        elif choice == "4":
            borrow_book(library)
            break
        elif choice == "5":
            return_book(library)
            break
        elif choice == "6":
            search_book(library)
            break
        elif choice == "7":
            break
        else:
            print("잘못된 메뉴입니다.")
            break

if __name__ == '__main__':
        main()


```
  -----
- 현재까지 class들과 method도 어느정도 구현이 완료되었지만, 아직 상속에 대해 헷갈리는 점이 있어서 좀 더딘 부분이 없지않아 있다.


 
